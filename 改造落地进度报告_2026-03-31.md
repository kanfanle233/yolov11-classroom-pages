# 正式执行版改造清单：落地进度报告（2026-03-31）

## 总结
- 本次已把主链切到固定 schema 流程，并落地了 `event_queries.jsonl`、`pose_tracks_smooth_uq.jsonl`、`verified_events.jsonl` 三份核心产物。
- 旧链路相关脚本未删除，但已标记为 `legacy/experimental` 或降级为兼容输出。
- `Sprint11`（终版 UQ）仍未开始。

## 按 Sprint 状态
1. Sprint1 主链收缩：**部分完成**
   - 已重写 `scripts/09_run_pipeline.py` 为正式主链（固定 schema + verifier 输出）。
   - 新增 `scripts/09b_run_pipeline.py` legacy 包装器（默认仅提示，不执行）。
   - `scripts/04_complex_logic.py`、`scripts/000.py` 已添加 legacy 提示。
2. Sprint2 固定 schema：**完成（基础版）**
   - 新增 `contracts/`：
     - `contracts/schemas.py`
     - `contracts/__init__.py`
   - 新增 schema 校验入口：
     - `scripts/02b_check_jsonl_schema.py`
   - 主链脚本已在关键步骤调用 schema 校验。
3. Sprint3 统一 ASR：**完成（基础版）**
   - 重写：
     - `scripts/06_api_asr_realtime.py`
     - `scripts/06_asr_whisper_to_jsonl.py`
   - 新行为：无论 API/依赖/识别是否失败，都会写出非空 `transcript.jsonl`（占位段）。
4. Sprint4 文本事件化：**完成（第一版）**
   - 新增 `scripts/06b_event_query_extraction.py`
   - 已接入主链，输出并校验 `event_queries.jsonl`。
5. Sprint5 UQ：**完成（第一版）**
   - 新增 `scripts/03c_estimate_track_uncertainty.py`
   - 已接入主链，输出 `pose_tracks_smooth_uq.jsonl`，并补充 `uq_*` 字段。
6. Sprint6 自适应对齐：**完成（第一版）**
   - 新增 `scripts/xx_align_multimodal.py`（主链版本）
   - 根据 motion + uq 自适应窗口，输出 `align_multimodal.json`。
7. Sprint7 verifier：**完成（骨架版）**
   - 新增 `verifier/`：
     - `verifier/contracts.py`
     - `verifier/model.py`
     - `verifier/dataset.py`
     - `verifier/train.py`
     - `verifier/infer.py`
     - `verifier/__init__.py`
8. Sprint8 Step07 重写：**完成（第一版）**
   - 重写 `scripts/07_dual_verification.py`
   - 已改为消费 `event_queries + pose_uq + align`，输出 `verified_events.jsonl`。
   - 可选兼容导出 `per_person_sequences.json`（`--per_person_out`）。
9. Sprint9 消费层：**完成（第一版）**
   - 修改 `scripts/10_visualize_timeline.py`：新增 `--verified_src`，可直接可视化 `verified_events` 与 `reliability`。
   - 修改 `server/app.py`：
     - `timeline` 支持从 `verified_events.jsonl` 回退构建。
     - `transcript` 接口可联动返回 `verified/reliability/verification_label`。
10. Sprint10 视觉证据核查：**完成（第一版）**
    - 重写 `scripts/05_slowfast_actions.py`
    - 新增 `--model_mode {auto,slowfast,rules}`，支持 SlowFast 不可用时自动切到规则 baseline。
    - 修改 `scripts/intelligence_class/pipeline/04_action_rules.py`，新增 baseline 标记输出。
11. Sprint11 终版 UQ：**未开始**

## 本次关键变更文件
- 新增：
  - `contracts/schemas.py`
  - `contracts/__init__.py`
  - `verifier/*`
  - `scripts/03c_estimate_track_uncertainty.py`
  - `scripts/06b_event_query_extraction.py`
  - `scripts/09b_run_pipeline.py`
  - `scripts/xx_align_multimodal.py`
- 重点重写：
  - `scripts/05_slowfast_actions.py`
  - `scripts/06_api_asr_realtime.py`
  - `scripts/06_asr_whisper_to_jsonl.py`
  - `scripts/07_dual_verification.py`
  - `scripts/09_run_pipeline.py`
  - `scripts/02b_check_jsonl_schema.py`
- 消费层更新：
  - `scripts/10_visualize_timeline.py`
  - `server/app.py`
- legacy 标记：
  - `scripts/000.py`
  - `scripts/04_complex_logic.py`
  - `scripts/intelligence_class/tools/xx_align_multimodal.py`

## 说明
- 环境内未检测到可用 `python/py` 命令，未完成本地 `compileall` 语法检查与端到端跑链验证。
- 下一步建议先在你机器上跑一次：
  - `scripts/09_run_pipeline.py --video ... --out_dir ...`
  - 然后检查输出目录中三份核心文件是否齐全且通过 `scripts/02b_check_jsonl_schema.py`。
