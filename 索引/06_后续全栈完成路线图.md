# 06 后续全栈完成路线图

更新时间：2026-05-04

## 当前完成度总览

| 主题 | 当前状态 | 说明 |
| --- | --- | --- |
| 主流程编排 | 已落地 | `scripts/main/09_run_pipeline.py` 可跑主线 |
| Hybrid 身份绑定 | 已落地 | `actions.fusion_v2.jsonl` 不再允许 `track_id >= 200000` |
| 后排 SR/切片消融 | 已落地 | `front_001/front_002/front_046` 均有汇总行 |
| `front_046` formal GT | 已落地 | 当前 formal 主 case |
| Frontend bundle | 已落地 | 已有 6 个 bundle case |
| Bundle API | 已落地 | `/api/bundle/*` 已存在 |
| BFF endpoint | 已落地 | `/api/v1/visualization/case_data` 已存在 |
| `index_v2.html` | 已有代码 | 已接 BFF，但是否默认启用仍需按页面整合判断 |
| 音频上下文增强 | 已有脚本与样本 | 样本量不足，仍偏系统分析 |
| LLM 语义融合 | 已有脚本与 37 对样本 | 可做定性与系统分析，不宜写成充分 benchmark |
| OCR / 敏感文本 | 未实现 | 当前应保留在未来工作 |

## 当前最值得推进的缺口

### P0

| 任务 | 原因 |
| --- | --- |
| 补 `front_001/front_002` 的 rear-row formal GT | 让多 case 不再只有 proxy 结论 |
| 重新审视 test split 口径 | 当前是图像级分层随机拆分，不是视频级分组 |

### P1

| 任务 | 原因 |
| --- | --- |
| 扩展有语音的长视频 case | 当前音频样本太少，无法把“音视频双重验证”写成强结论 |
| 把 LLM 语义样本扩到更大规模 | 37 对样本目前更像分析集，而不是 benchmark |
| 扩展 `front_046` GT 的学生覆盖 | 当前 precision 受 GT 覆盖有限影响明显 |

### P2

| 任务 | 原因 |
| --- | --- |
| 让 `index_v2.html` 与 bundle/BFF 形成稳定演示闭环 | 代码已存在，但默认展示与录屏路径仍需统一 |
| 增强 `failure_cases.json`、formal 指标面板与证据视图 | 当前 bundle 结构已具备接口基础 |

## 当前推荐工作顺序

1. 保持 `front_046` 作为当前 formal 主样本。
2. 补 `front_001/front_002` 的 rear-row GT，避免论文只有单 case formal。
3. 对音频部分优先补数据，不优先堆复杂算法。
4. 用扩展后的 formal 结果重新评估 `A1/A3/A7/A8`，确认是覆盖优先还是稳定性优先。
5. 把 `index_v2.html`、BFF、bundle 的展示链做成统一演示面。

## 当前最稳的论文主线

- 检测主线：行为检测模型与 test 指标。
- 系统主线：主流程、hybrid 身份绑定、Pipeline Contract。
- 后排增强主线：SR/切片消融，且明确区分 proxy 与 formal。
- 前端主线：bundle API + D3 页面 + BFF endpoint 已存在。

## 当前不应抢先完成化的内容

- 不应把音频/LLM 写成充分闭环的主 benchmark。
- 不应把 `index_v2.html` 直接写成最终成品前端。
- 不应把 OCR/敏感文本、底层 YOLO 增强训练结果写成已完成。

## 当前阶段最合理的完成定义

| 级别 | 当前判断 | 说明 |
| --- | --- | --- |
| 工程可跑通 | 已达到 | 主流程、消融、bundle、API 均已存在 |
| 论文可写 | 部分达到 | `front_046` formal 可写，其他 case 仍偏 proxy |
| 展示闭环 | 部分达到 | `paper_demo.html`、bundle、BFF 已有，但 `index_v2` 整合还未完全定型 |

## 关键路径产物

| 产物 | 路径 |
| --- | --- |
| 主流程入口 | `scripts/main/09_run_pipeline.py` |
| SR 消融脚本 | `scripts/experiments/16_run_rear_row_sr_ablation.py` |
| GT 模板脚本 | `scripts/experiments/18_build_rear_row_gt_template.py` |
| Formal 评估脚本 | `scripts/experiments/19_eval_rear_row_metrics.py` |
| Frontend 打包脚本 | `scripts/frontend/20_build_frontend_data_bundle.py` |
| Bundle API / BFF | `server/app.py` |
| BFF 页面模板 | `web_viz/templates/index_v2.html` |
