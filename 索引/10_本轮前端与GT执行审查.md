# 10 本轮前端与 GT 执行审查

## 审查结论

本轮工作整体符合 `06_后续全栈完成路线图.md` 中 P1、P7、P8 的方向，但只能算“工程准备完成”，还不能算“正式论文指标闭环完成”。原因是 GT 模板已经生成，但 `persons[]/actions[]` 仍为空，必须人工标注后才能进入 P2-P6。

## 已符合要求的部分

| 要求 | 当前状态 | 证据 |
| --- | --- | --- |
| 选择足够长的视频做 GT | 已修正为 `046.mp4`，约 41.3 秒 | `output/codex_reports/rear_gt/front_046_rear_gt.report.json` |
| 生成 GT 模板 | 已生成 42 帧，每秒 1 帧 | `output/codex_reports/rear_gt/front_046_rear_gt.jsonl` |
| 前端统一数据 bundle | 已生成 `front_002_A8` bundle | `output/frontend_bundle/front_002_A8/frontend_data_manifest.json` |
| 前端数据不直接读大 JSONL | 已抽样 tracks，CSV/JSONL 转 JSON | `timeline_students.json`、`tracks_sampled.json` |
| D3 时间线 | 已有 Gantt 行为时间线 | `web_viz/templates/paper_demo.html` |
| D3 消融图 | 已有指标柱状图 | `web_viz/templates/paper_demo.html` |
| 后端 bundle API | 已新增并验证 200 | `/api/bundle/front_002_A8/*` |
| 页面入口 | 已新增并验证 200 | `/paper/bundle/front_002_A8` |
| 046 消融完整流程 | 已完成 6 个实际变体，全部 contract ok | `output/codex_reports/front_046_sr_ablation/sr_ablation_metrics.json` |
| 046 前端数据 bundle | 已生成并验证 `front_046_A8` | `output/frontend_bundle/front_046_A8/frontend_data_manifest.json` |

## 本次发现并已修复的问题

| 问题 | 影响 | 修复 |
| --- | --- | --- |
| `front_002` 视频只有 7.6 秒 | 不满足 30-60 秒 GT 标注目标 | 改用 `正方视角/046.mp4`，约 41.3 秒 |
| GT 模板帧目录共用 `rear_gt/gt_frames` | 多个视频的参考帧会互相覆盖 | `18_build_rear_row_gt_template.py` 默认改为 `front_xxx_rear_gt/gt_frames` |
| D3 消融图读取 `variant_id/id`，但数据字段是 `variant` | 柱状图名称为空或重叠 | `paper_demo.html` 改为兼容 `case + variant/variant_id/id` |
| 前端 8 类行为中文标签乱码 | 页面展示不可读 | 改为正常中文标签：抬头听课、低头看书等 |
| 时间线不能跳转视频 | 前端不能定位到视频帧 | 点击 Gantt 段会跳到 `start_time` 并播放 |
| bundle 脚本空 artifact 路径不安全 | manifest 缺字段时可能读项目根目录 | 加 `artifact_path` 和 `is_file` 检查 |
| bundle API 端点不完整 | 前端无法通过 API 读 behavior/fusion/failure 数据 | 增加 `behavior_segments`、`fusion_segments`、`student_id_map`、`failure_cases` API |
| case_id 未限制字符 | 有路径穿越风险 | 增加正则校验 `[A-Za-z0-9_.-]+` |

## 当前仍未完成的内容

| 阻塞项 | 为什么阻塞 | 下一步 |
| --- | --- | --- |
| 人工 GT 标注未填写 | formal 指标仍不能计算 | 填写 `front_046_rear_gt.jsonl` 的 `persons[]`，至少 bbox + track_id |
| 正式指标仍为空 | `gt_status=missing` 或 GT persons 为 0 | 标注后运行 `19_eval_rear_row_metrics.py` |
| 失败案例数据为空 | 还没有 GT 错误分类结果 | P2 后生成 `failure_cases.json` |
| 前端仍是 demo 页面 | 尚未做完整多页产品化 | 后续拆成总览页、指标页、失败页、数据说明页 |
| 行为/姿态正式阈值未验证 | 缺 GT | P2-P4 根据正式指标调参 |
| A8 是否最终最优未证明 | A8 学生数最多，但 gap proxy 也最高 | 与 A7/A1 一起用 GT 指标比较 |

## 当前关键路径

1. 人工标注 `output/codex_reports/rear_gt/front_046_rear_gt.jsonl`。
2. 至少填写每帧后排 ROI 内 `bbox + track_id`。
3. 可选填写 `behavior_code/keypoints/actions`，用于行为 F1、PCK/OKS、temporal mAP。
4. 标注完成后运行正式评估。
5. 将评估结果重新打包到 frontend bundle。
6. 前端展示 formal 指标、proxy 指标、失败案例。

## 标注后命令

```powershell
& F:/miniconda/envs/pytorch_env/python.exe ./scripts/19_eval_rear_row_metrics.py --case_dir output/codex_reports/front_046_sr_ablation/A8_adaptive_sliced_artifact_deblur_opencv --gt_jsonl output/codex_reports/rear_gt/front_046_rear_gt.jsonl --video data/智慧课堂学生行为数据集/正方视角/046.mp4 --roi auto_rear
```

注意：当前 `front_002_A8` bundle 来自 `002.mp4`，不能直接拿 `046.mp4` 的 GT 去评估 `002.mp4` 的 case。正式做 P2 时需要先用 `046.mp4` 跑完整 A0/A1/A2/A3/A8 消融，生成 `front_046_sr_ablation` 后再评估。

## 前端验证结果

已用 FastAPI TestClient 验证：

| 路径 | 状态 |
| --- | --- |
| `/api/bundle/list` | 200 |
| `/api/bundle/front_002_A8/manifest` | 200 |
| `/api/bundle/front_002_A8/timeline` | 200 |
| `/api/bundle/front_002_A8/metrics` | 200 |
| `/api/bundle/front_002_A8/ablation` | 200 |
| `/api/bundle/front_002_A8/behavior_segments` | 200 |
| `/api/bundle/front_002_A8/fusion_segments` | 200 |
| `/api/bundle/front_002_A8/failure_cases` | 200 |
| `/paper/bundle/front_002_A8` | 200 |

新增 `front_046_A8` 验证：

| 路径 | 状态 |
| --- | --- |
| `/api/bundle/front_046_A8/manifest` | 200 |
| `/api/bundle/front_046_A8/timeline` | 200 |
| `/api/bundle/front_046_A8/tracks_sampled` | 200 |
| `/api/bundle/front_046_A8/metrics` | 200 |
| `/api/bundle/front_046_A8/ablation` | 200 |
| `/api/bundle/front_046_A8/behavior_segments` | 200 |
| `/api/bundle/front_046_A8/fusion_segments` | 200 |
| `/api/bundle/front_046_A8/failure_cases` | 200 |
| `/paper/bundle/front_046_A8` | 200 |

## 046 消融后更新判断

`046.mp4` 已经不再阻塞“完整 case 是否存在”：`front_046_sr_ablation` 已包含可用的 A0/A1/A2/A3/A7/A8 结果。当前阻塞点变为“同一视频的人工 GT 是否完成”。没有 GT 时，A8 只能说明覆盖更多学生，不能说明正式识别准确率或跟踪身份质量最好。

## 结论

本轮新增工作作为 P1/P7/P8 的工程准备是合格的；我已修复会影响后续标注和前端展示的关键问题。下一步不要急着宣布 P2 完成，必须先用 `046.mp4` 跑完整后端 case，再用同一个视频的 GT 评估，否则会出现“GT 与 case 视频不一致”的严重实验错误。

