# 10 本轮前端与 GT 执行审查

更新时间：2026-05-04

## 审查结论

本轮最重要的状态变化已经发生：

- `front_046` 不再是“待补 GT”，而是当前 formal 主 case。
- bundle API 与 BFF endpoint 都已经在仓库里落地。
- `index_v2.html` 已存在并直接接入 BFF。

因此，这一轮的真实结论不是“还在准备阶段”，而是：

- 前后端与 GT 的基础链路已经打通。
- 仍然缺的是多 case formal、音频样本扩展，以及更完整的前端交互闭环。

## 已完成项

| 项目 | 当前状态 | 证据 |
| --- | --- | --- |
| `front_046` GT | 已完成并用于 formal | `output/codex_reports/rear_gt/front_046_rear_gt.report.json` |
| `front_046` formal 指标 | 已进入 SR 汇总表 | `output/codex_reports/sr_ablation_paper_summary.json` |
| `front_046_A8` bundle | 已完成 | `output/frontend_bundle/front_046_A8/frontend_data_manifest.json` |
| Bundle API | 已完成 | `server/app.py` |
| BFF endpoint | 已完成 | `server/app.py` |
| `index_v2.html` | 已存在并接 BFF | `web_viz/templates/index_v2.html` |
| `paper_demo.html` | 已可走单 case 演示 | `web_viz/templates/paper_demo.html` |

## 本轮关闭的旧阻塞项

| 旧阻塞项 | 当前判断 |
| --- | --- |
| “046 还没有 GT” | 已关闭 |
| “formal 指标还不能算” | 已关闭，但仅对 `front_046` 成立 |
| “前端只有静态方案文档” | 已关闭，bundle API/BFF/模板都已存在 |

## 当前仍然存在的阻塞项

| 阻塞项 | 影响 |
| --- | --- |
| `front_001/front_002` 仍缺 rear-row formal GT | 多 case formal 不完整 |
| 音频有效 case 太少 | 音频上下文增强不能写成强 benchmark |
| `index_v2.html` 仍未被明确收敛为默认演示页 | 前端叙事有双轨 |
| `failure_cases.json` 内容仍偏薄 | 错误分析页还不够强 |

## 当前前端口径应如何写

- `paper_demo.html`：已落地、可展示。
- Bundle API：已落地、可展示。
- BFF endpoint：已落地、可展示。
- `index_v2.html`：已有代码与 BFF 接口，但仍属于下一阶段展示页，不应写成完全替代旧页。

## 当前 GT 口径应如何写

- `front_046`：当前 formal 主样本。
- `front_002`：保留为历史模板与 proxy case。
- 不允许把 `front_046` GT 与 `front_002` case 混用。

## 本轮最重要的后续动作

1. 给 `front_001/front_002` 增加 formal GT。
2. 让 `index_v2.html` 与 bundle/BFF 形成固定演示路径。
3. 扩展 failure cases 与证据可视化。
4. 扩展有语音 case，而不是先继续放大语义策略复杂度。
