# 11 全实验数据与论文指标记录

更新时间：2026-05-04

## 当前实验面概览

| 模块 | 当前状态 | 当前可用性 |
| --- | --- | --- |
| 行为检测 test 指标 | 已完成 | 可直接写论文 |
| 后排 SR/切片消融 | 已完成 | proxy + formal 混合可用 |
| `front_046` formal GT | 已完成 | 可直接写论文 |
| 跟踪消融 | 已完成 | 可直接写论文 |
| Frontend bundle | 已完成 | 可展示、可录屏 |
| Bundle API | 已完成 | 可展示 |
| BFF endpoint | 已完成 | 可展示 |
| 音频上下文增强 | 已有样本与脚本 | 可做系统分析 |
| LLM 语义融合 | 已有 37 对样本 | 可做定性与统计分析 |
| OCR / 敏感文本 | 未实现 | 不可写成已完成 |

## 1. 行为检测主结果

| 指标 | 值 |
| --- | ---: |
| Test mAP50 | 0.9806 |
| Test mAP50-95 | 0.8782 |
| Precision | 0.9571 |
| Recall | 0.9370 |

边界：

- 可以写成主检测结果。
- 需要同时说明当前 split 不是视频级分组拆分。

## 2. 后排增强结果

### 2.1 当前 formal 主 case：`front_046`

| 最强覆盖变体 | tracked_students | 最佳 formal recall 变体 | person_recall |
| --- | ---: | --- | ---: |
| A8 | 37 | A3 | 0.1500 |

### 2.2 当前 proxy 结论

- `front_001/front_002/front_046` 都表明切片/后排增强能显著提高候选覆盖与行为挂载。
- `front_001/front_002` 仍然只能写 proxy，不承担 formal accuracy 结论。

## 3. 跟踪消融结果

| 对比 | 当前结论 |
| --- | --- |
| TK-0 -> TK-1 | 匈牙利匹配减少碎片与大间隙 |
| TK-1 -> TK-2 | Kalman 在当前设置下无额外收益 |
| 自定义 -> ByteTrack | ByteTrack 更平滑，但检测保守 |

当前写作建议：

- TK-1 可作为当前主线推荐。
- TK-2 的“无收益”应诚实保留为负结果。

## 4. Frontend / API / 展示资产

### 已存在的 bundle case

| case | students | gt_status |
| --- | ---: | --- |
| front_002_A8 | 31 | missing |
| front_046_A8 | 37 | ok |
| front_1885_sliced | 50 | missing |
| front_22259_sliced | 44 | missing |
| front_26729_sliced | 50 | missing |
| front_45618_sliced | 45 | missing |

### 已存在的接口

- `/api/bundle/*`
- `/api/v1/visualization/case_data`
- `/paper/bundle/{case_id}`

### 已存在的模板

- `paper_demo.html`
- `index_v2.html`

## 5. 音频与 LLM 当前状态

### 音频

- 当前 `output/asr_test_*` 已覆盖 5 个视频目录。
- `multi_case_data.json` 显示仅 2 个 case 具有教师指令事件。
- 当前最稳妥的定位是“上下文增强”，不是“已充分成立的双重验证主结果”。

### LLM

- 当前统计基础为 37 对指令-行为样本。
- 可写为“系统分析/定性增强”，不应超前写成强 benchmark。

## 6. 当前可写入论文的内容

- 行为检测 test 指标。
- `front_046` 的 formal person/tracking/behavior 指标。
- 后排切片/SR 带来的 proxy 覆盖提升。
- 跟踪消融结论。
- bundle、bundle API、BFF endpoint、前端演示能力。
- 音频上下文增强与 LLM 语义融合的系统设计与初步证据。

## 7. 当前暂不能写入论文的内容

- 把 `front_001/front_002` proxy 写成 formal accuracy。
- 把 `A8` 直接写成总体最优。
- 把音频/LLM 写成样本充分的 benchmark 主结果。
- 把 OCR/敏感文本写成已实现能力。
- 把未训练闭环的底层 YOLO 增强模块写成实验结果。

## 8. 当前最需要补的实验

1. `front_001/front_002` 的 rear-row formal GT。
2. 更多有语音的长视频 case。
3. 更大规模的 LLM 语义样本。
4. 若继续主打检测结果，需要收紧 test split 风险表述或补更严谨拆分。

## 关键产物路径

| 产物 | 路径 |
| --- | --- |
| SR 汇总总表 | `output/codex_reports/sr_ablation_paper_summary.json` |
| `front_046` GT 报告 | `output/codex_reports/rear_gt/front_046_rear_gt.report.json` |
| 多 case 汇总 | `output/frontend_bundle/multi_case_data.json` |
| Bundle 清单 | `output/frontend_bundle/*/frontend_data_manifest.json` |
